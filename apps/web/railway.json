{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @repo/database db:generate && pnpm --filter @repo/database db:deploy && pnpm build:web"
  },
  "deploy": {
    "startCommand": "pnpm start:web",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "watch": ["apps/web/**", "packages/database/**"]
}
